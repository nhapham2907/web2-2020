'use strict';
// const { sanitizeEntity } = require("strapi-utils");

module.exports = {
  // async find(ctx) {
  //   let entities;
  //   if (ctx.query._q) {
  //     entities = await strapi.services['interest-rate'].search(ctx.query);
  //   } else {
  //     entities = await strapi.services['interest-rate'].find(ctx.query);
  //   }

  //   return entities.map((entity) =>
  //     sanitizeEntity(entity, { model: strapi.models['interest-rate'] })
  //   );
  // },
};